{
  "name": "devextreme-cli-monorepo",
  "version": "1.1.10",
  "scripts": {
    "test": "lerna run --stream test",
    "test-local": "lerna run --parallel test-local",
    "watch": "lerna run --stream test-dev"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}

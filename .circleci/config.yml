version: 2
jobs:
  build:
    docker:
      - image: circleci/node:11
    steps:
      - checkout
      - run:
          name: Install npm packages
          command: npx lerna bootstrap
      - run:
          name: Run test
          command: npx lerna test

version: 2
jobs:
  build:
    docker:
      - image: circleci/node:11
    steps:
      - checkout
      - run:
          name: Install npm packages
          command: npm install
      - run:
          name: Run lint
          command: npm run lint
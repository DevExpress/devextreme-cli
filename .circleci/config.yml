version: 2
jobs:
  build:
    docker:
      - image: circleci/node:11
    steps:
      - checkout
      - run:
          name: Install npm packages
          command: npm i
      - run:
          name: Run test
          command: npm run test
      - store_artifacts:
          path: test-results.xml
          prefix: tests
